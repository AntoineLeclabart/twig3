{% extends 'layout.html.twig' %}
{% block content %}
    {% for product in products %}
        {% if loop.index < 4 %}
            {{product}}
        {% endif %} 
    {% endfor %}
    <br/>
    {% for product in products %}
        {% if loop.index > 3 %}
            {{product}}
        {% endif %} 
    {% endfor %}
    {{dump(products)}}
{% endblock %}
